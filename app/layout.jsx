export const metadata = {
  title: "AI Product Image Generator",
  description: "Generate AI product images easily",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
